import { initComponent } from 'lib/components'
import CollectionCarousel from 'modules/collection-carousel/collection-carousel.js'

document.addEventListener('DOMContentLoaded', () => {
  initComponent(CollectionCarousel, 'CollectionCarousel')
})
