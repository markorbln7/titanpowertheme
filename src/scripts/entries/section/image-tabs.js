import { initComponent } from 'lib/components'
import ImageTabs from 'modules/image-tabs/section-image-tabs.js'

document.addEventListener('DOMContentLoaded', () => {
  initComponent(ImageTabs, 'ImageTabs')
})
