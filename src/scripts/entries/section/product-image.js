import { initComponent } from 'lib/components'
import ProductImage from 'modules/product-image/product-image.js'

document.addEventListener('DOMContentLoaded', () => {
  initComponent(ProductImage, 'ProductImage')
})
